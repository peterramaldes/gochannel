package main

import (
	"testing"
)

func TestAddUser(t *testing.T) {
	// server := NewServer()
	// server.Start()

	// for i := 0; i < 10; i++ {
	// 	go func(i int) {
	// 		server.userch <- fmt.Sprintf("user_%d", i)
	// 	}(i)
	// }

	// fmt.Println("the loop is done")
	// // b, _ := json.MarshalIndent(server.users, "", " ")
	// // fmt.Println(string(b))
}
